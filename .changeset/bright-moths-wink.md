---
'@sl-design-system/form': patch
---

Improve `<sl-form-validation-errors>` so validation errors are announced in a more accessible way after replacing the previous list with links to invalid fields, which was not accessible, with cleaner comma spacing between multiple invalid field labels.
