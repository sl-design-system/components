---
'@sl-design-system/calendar': patch
---

Updated `MonthView`, `SelectMonth`, and `SelectYear` components to improve accessibility for disabled items. Disabled dates, months, and years are now focusable via keyboard navigation (using `aria-disabled="true"`) but remain non-interactive
