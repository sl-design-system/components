---
'@sl-design-system/form': minor
---

Add `announce-errors` to `<sl-form>` to control field-level validation announcements.

`announce-errors` is enabled by default. Set `announce-errors="false"` when using aggregated error messaging (for example with `<sl-inline-message>`) to avoid duplicate screen reader announcements. Form-field level announcement control is internal.
