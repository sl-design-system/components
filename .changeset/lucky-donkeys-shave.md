---
'@sl-design-system/checkbox': patch
'@sl-design-system/radio-group': patch
---

Fix the alignment of an `sl-infotip` slotted into the label of an `sl-checkbox` or `sl-radio` with non-default sizes.
