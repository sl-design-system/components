import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Wrappers/Grid/Grouping" />

# Grid Grouping

This section shows grouping patterns in the Angular Grid wrapper.

Use these examples to group related columns under shared headers.
