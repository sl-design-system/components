import { LitElement, type TemplateResult, html } from 'lit';

export class FilterCheckboxGroup extends LitElement {
  override render(): TemplateResult {
    return html`Filter checkbox group`;
  }
}
