{
  "name": "@slds/scripts",
  "license": "ISC",
  "description": "Build scripts for SLDS monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "globby": "^13.1.2"
  }
}
